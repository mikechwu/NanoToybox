/**
 * Coachmark definitions — onboarding copy and IDs.
 * main.js decides when to show them; this module owns what they say.
 */
export const COACHMARKS = {
  placement: { id: 'placement', text: 'Tap Place to add it' },
};
